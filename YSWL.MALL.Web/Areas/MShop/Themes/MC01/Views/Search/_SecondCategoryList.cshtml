@model List<YSWL.MALL.Model.Shop.Products.CategoryInfo>
@if (Model != null && Model.Count > 0)
{
     for (int i =0; i < Model.Count; i++)
      {
        <li   cid="@Model[i].CategoryId" class="cateValues"><a href="javascript:;"><span class="b_item s_sort">@Model[i].Name</span></a></li>
      }
}
 