@model List<YSWL.MALL.Model.Shop.ReturnOrder.ReturnOrderAction>
              
                <table cellpadding="0" cellspacing="0" width="100%">
                    <tbody id="tbody_track">
                        <tr>
                            <th style="width:140px;">
                                <strong>处理时间</strong>
                            </th>
                            <th>
                                <strong>处理信息</strong>
                            </th>
                            <th style="width:100px;">
                                <strong>操作人</strong>
                            </th>
                        </tr>
        @if (Model != null)
           {
               foreach (var item in Model)
               {
                   <tr>
                            <td>
                             @item.ActionDate.ToString("yyyy-MM-dd HH:mm:ss")  
                            </td>
                            <td>
                              @item.Remark
                            </td>
                            <td>
                                 @item.UserName
                            </td>
                        </tr>
               }


           }
                    
                    </tbody>
                </table>
         

