 @if (Model != null && Model.Count > 0)
 {
        for (int i = 0; i < Model.Count; i++)
        {

        <li class="fl"><a href="@(ViewBag.BasePath)Store/ListV2/@(Model[i].SupplierId)/@Model[i].CategoryId/hot"><span>@Model[i].Name</span></a></li>
        }
 }