@using Webdiyer.WebControls.Mvc
@model PagedList<YSWL.MALL.Model.Members.PointsDetail>
    

@if (Model != null && Model.Count > 0)
{
    foreach (var item in Model)
    {
        if (item.Type == 0)
        {
            <p><span class="integralTxt">@item.Description</span><span class="integralTime">@item.CreatedDate.ToString("yyyy-MM-dd HH:mm:ss")</span><span class="integralNum red">+@item.Score</span></p>
        }
        else
        {
            <p><span class="integralTxt">@item.Description</span><span class="integralTime">@item.CreatedDate.ToString("yyyy-MM-dd HH:mm:ss")</span><span class="integralNum">-@item.Score</span></p>
        }
    }
}